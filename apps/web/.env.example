# Local dev server port (used by `pnpm dev`, Electron, and root `pnpm dev:desktop`).
# If unset, the app defaults to 3000. Copy to `.env` / `.env.local` to override (e.g. 3017). See README.
PORT=3017
